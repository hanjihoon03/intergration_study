# database.yml
spring:
  datasource:
    url:
      auth-service: jdbc:mysql://localhost:3306/intergration_auth
      product-service: jdbc:mysql://localhost:3306/intergration_product
      order-service: jdbc:mysql://localhost:3306/intergration_order
    username: root
    password:
      auth-service: raik1353  # auth-service에 맞는 비밀번호
      product-service: raik1353  # product-service에 맞는 비밀번호
      order-service: raik1353  # order-service에 맞는 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto:
        auth-service: create
        product-service: create
        order-service: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 30
